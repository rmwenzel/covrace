---
title: "Job Resource Estimation and Analysis"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```


Get number of rows/blockgroups for each state

```{r}
dem <- read.csv('data/us-dem-counts-jan-2021-with-neighbors.csv', header=TRUE, colClasses=c("spatial_id"="character"))
head(dem)
```

Estimate memory resource requirements

```{python}

```